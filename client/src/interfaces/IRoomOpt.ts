export interface IRoomOpt {
  bed: string;
  id: number;
  isRefundable: boolean;
  name: string;
  numberOfGuest: number;
  originalPrice: number;
  price: number;
  roomId: number;
  roomLeft: 32;
}
